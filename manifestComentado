{
  "manifest_version": 3,  // Versão do manifesto (obrigatório ser 3 em manifestos V3).
  "name": "Mantis2Trello Atualizado",  // Nome da extensão.
  "version": "1.0",  // Versão da extensão.
  "description": "",  // Descrição da extensão (pode ser preenchida com informações sobre a extensão).

  // Permissões requeridas pela extensão (anteriormente listadas sob "permissions").
  "permissions": [
    "activeTab",  // Permissão para acessar a guia ativa do navegador.
    "storage"  // Permissão para armazenar dados na área de armazenamento local.
  ],

  // Configurações do serviço de fundo (anteriormente "background").
  "background": {
    "service_worker": "background.js"  // Define o serviço de fundo usando um arquivo JavaScript.
  },

  // Ícones da extensão em diferentes tamanhos.
  "icons": {
    "16": "Mantis2TrelloIcon16.png",
    "48": "Mantis2TrelloIcon48.png",
    "128": "Mantis2TrelloIcon128.png"
  },

  // Ação da extensão (anteriormente "page_action").
  "action": {
    "default_popup": "popup.html",  // Define a página pop-up padrão quando a extensão é clicada.
    "default_icon": {  // Define o ícone padrão para a extensão.
      "16": "Mantis2TrelloIcon16.png",
      "48": "Mantis2TrelloIcon48.png",
      "128": "Mantis2TrelloIcon128.png"
    }
  },

  // Página de opções da extensão (anteriormente "options_page").
  "options_ui": {
    "page": "options.html",  // Define a página de opções da extensão.
    "open_in_tab": true  // Abre a página de opções em uma nova guia do navegador.
  },

  // Conteúdo a ser injetado nas páginas da web correspondentes.
  "content_scripts": [
    {
      "matches": [
        "*://*/mantis/view.php*",  // URLs correspondentes onde o conteúdo será injetado.
        "*://*/bugs/view.php*",
        "*://*/view.php*"
      ],
      "js": [  // Lista de scripts JavaScript para serem injetados.
        "jquery.3.7.1.min.js",
        "client.js",
        "content.js"
      ],
      "run_at": "document_end"  // Momento em que os scripts serão executados (após o carregamento da página).
    }
  ]
}
